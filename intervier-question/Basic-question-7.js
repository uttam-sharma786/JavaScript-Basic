// Params vs Arguments

function square(num) { // parama or parameter
    console.log(num * num );
}
square(5) // this is arguments

// when we passed value lime square(5) that are called arguments
// When we receive those value inside of our function this is called params
// like here (num ) this is params